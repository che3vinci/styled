import { DepTree } from '../tree';
import { StyledElement } from '../component/StyledElement';

/**
 * 组件的调用路径.如果在tree中已经存在该节点，则
 * @param element
 * @param trees
 */
export const getCallPath = (element: StyledElement, trees: DepTree[]) => {};
