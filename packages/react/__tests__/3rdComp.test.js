test.skip('xx',()=>{

    //如果第三组件不支持className属性呢?
    const _3rdComp = styled(AntdButton,{
        
    })
    expect('')

})

/**
 * const StyledButton  = styled('button',{});
 * const X = ()=>{
 *   return <StyledButton>login</StyledButton>
 * }
 * const Y = styled(X,{})
 */